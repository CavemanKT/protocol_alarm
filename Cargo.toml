[package]
name = "protocol_alarm_gui"
version = "0.1.0"
edition = "2024"

[dependencies]
iced = { version = "0.13", features = ["tokio"] }
chrono = "0.4"
tokio = { version = "1", features = ["rt-multi-thread", "time", "macros"] }